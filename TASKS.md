# Задачи · ОКНА CRM

Бэклог по итогам ревью кода и запуска на `crm.oceanglass.kg`.
Статус: `[ ]` — не начато, `[~]` — в работе, `[x]` — готово.

## Баги / нестыковки

- [ ] **Демо-гейт на собственном домене.** На `crm.oceanglass.kg` голый
  вход упирается в гейт «Доступ к демо по ссылке» (нужен `?demo=…`/`?owner=…`).
  Раз это собственный домен клиента — пропускать гейт на этом хосте.
  → `initGate()` / `gateStatus()` в `data.js`.
- [x] **Дубликат имени сотрудника.** `u_pm` (менеджер) и `u_ps` (замерщик)
  оба «Платон Цай». → замерщик переименован в «Данияр Оспанов» в `buildSeed()`.
- [ ] **Расчёт опций по длине.** В `constrPrice` опции с `per:'м'` считаются
  как `2×ширина` (пометка `// periметр-ish`). Уточнить: подоконник/отлив — по
  ширине, откосы — по периметру проёма. → `constrPrice` в `data.js`.

## Функциональность

- [ ] **Рабочий поиск в топбаре.** Сейчас `data-act="noop"`. Сделать живой
  фильтр по клиентам и сделкам. → `renderShell()` в `ui.js`, action в `main.js`.
- [ ] **Уведомления (колокольчик).** Сейчас заглушка-`toast`. Завести реальную
  ленту событий из `DB.activity`. → action `notif` в `main.js`.
- [ ] **Экспорт/печать КП.** Кнопка печати или сохранения КП в PDF из модалки.
  → `openKp()` в `modules2.js`.

## Брендинг и контент

- [ ] **Брендинг под Ocean Glass.** Раз домен клиента — обновить название
  компании, логотип, цвета темы, мок-данные под Ocean Glass (г. Ош).
  → `buildSeed().company` в `data.js`, переменные в `styles.css`.
- [ ] **Проверить мобильную адаптивность** канбана воронки/производства и
  экрана замера на узких экранах (замерщик работает с планшета/телефона).

## Инфраструктура

- [x] Развёрнуто на `crm.oceanglass.kg` (Cloudflare, HTTPS, все ресурсы 200).
- [x] `CLAUDE.md` — карта проекта и гайд для разработки.
